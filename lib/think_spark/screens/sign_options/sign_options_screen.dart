import 'package:flutter/material.dart';

class SignOptionsScreen extends StatelessWidget {
  const SignOptionsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Sign Options Screen')),
    );
  }
}
